# Maintainer: nyfair <nyfair2012@gmail.com>
pkgname=libdvdcss-dev
pkgver=1.5.0
pkgrel=1
pkgdesc='Portable abstraction library for DVD decryption'
arch=('x86_64')
url="https://www.videolan.org/developers/libdvdcss.html"
license=('GPL')
source=("https://download.videolan.org/pub/libdvdcss/$pkgver/libdvdcss-$pkgver.tar.xz")
md5sums=('SKIP')

build() {
  cd $srcdir/libdvdcss-$pkgver
  mkdir work
  meson . work --prefix=/opt --buildtype=release --default-library=static
  cd work
  ninja
}

package() {
  cd $srcdir/libdvdcss-$pkgver/work
  DESTDIR=$pkgdir ninja install
  mv $pkgdir/msys64/* $pkgdir
  rm -rf $pkgdir/msys64
}
